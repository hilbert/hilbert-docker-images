export SHOW_CLOSE_BUTTON="${SHOW_CLOSE_BUTTON:-0}"
export BROWSER="${BROWSER:-/opt/kiosk.sh}"
# /usr/bin/firefox
export LANGUAGE="${LANGUAGE:-de}"
export LANG="${LANG:-de}"
# de,ru,fr etc.
export HIDE_MOUSE_CURSOR="${HIDE_MOUSE_CURSOR:-0}"
# 0 - to hide
